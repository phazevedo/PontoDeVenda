<div class = "row">
    <div class="form-group col-md-2">
        <%= label_tag "ID" %>
        <%= text_field_tag "id_search", nil, class: "form-control", onkeyup: "filter('id_search', 'client_table', 0)" %>
    </div>
    <div class="form-group col-md-6">
        <%= label_tag "Nome" %>
        <%= text_field_tag "name_search", nil, class: "form-control", onkeyup: "filter('name_search', 'client_table', 1)" %>
    </div>
</div>

<table class="table" id="client_table">
  <thead>
    <tr>
      <th>Código</th>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.id %></td>
        <td><%= client.name %></td>
        <td>
            <input type="button" value="Selecionar" class="btn btn-outline-success" data-dismiss="modal" onclick="populateField('client_id', <%= client.id %>)">
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
