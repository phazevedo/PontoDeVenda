<%= form_for(product_sale) do |f| %>
  <% if product_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_sale.errors.count, "error") %> prohibited this product_sale from being saved:</h2>

      <ul>
      <% product_sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :qty %>
    <%= f.text_field :qty %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
